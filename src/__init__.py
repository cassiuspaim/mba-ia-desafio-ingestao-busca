# src/__init__.py
"""
Main package initializer for the project.

This file ensures that all submodules under `src/` are importable
(e.g., `from embeddings import get_providers`).

It does **not** contain business logic — only import configuration
and optional version metadata.
"""

__version__ = "0.1.0"
__all__ = []
